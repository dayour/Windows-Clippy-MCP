<Window x:Class="WidgetHost.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        mc:Ignorable="d"
        Title="Clippy Bench"
        Width="540"
        Height="620"
        MinWidth="420"
        MinHeight="380"
        WindowStyle="None"
        Background="#FF0C0C0C"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="Manual">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <!-- Toolbar button (rounded pill) -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="11.5" />
            <Setter Property="Foreground" Value="#FFE8E8E8" />
            <Setter Property="Background" Value="#FF111122" />
            <Setter Property="BorderBrush" Value="#FF333355" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF1A1A35" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FF5B5FC7" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF24244A" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Chrome button (header) -->
        <Style x:Key="ChromeButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="30" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#FF707070" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF1A1A35" />
                                <Setter Property="Foreground" Value="#FFE8E8E8" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Mode toggle (A/P/S) -->
        <Style x:Key="ModeToggleStyle" TargetType="ToggleButton">
            <Setter Property="Height" Value="28" />
            <Setter Property="MinWidth" Value="28" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Foreground" Value="#FFB7B7D6" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="ToggleBorder"
                                Margin="1"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="8,0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleBorder" Property="Background" Value="#FF1A1A35" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="White" />
                                <Setter TargetName="ToggleBorder" Property="Background" Value="#FF5B5FC7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox item -->
        <Style x:Key="ToolbarComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#FFE8E8E8" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}" CornerRadius="6" Margin="4,2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#FF1A1A35" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#FF2D2D57" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toolbar ComboBox -->
        <Style x:Key="ToolbarComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#FFE8E8E8" />
            <Setter Property="Background" Value="#FF111122" />
            <Setter Property="BorderBrush" Value="#FF333355" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="11.5" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="10,0,24,0" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="MaxDropDownHeight" Value="280" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource ToolbarComboBoxItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="ComboBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8" />
                            <ToggleButton x:Name="DropDownToggle"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </ToggleButton.Template>
                                <Grid>
                                    <Border Width="22"
                                            HorizontalAlignment="Right"
                                            Background="#FF181832"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="1,0,0,0"
                                            CornerRadius="0,8,8,0">
                                        <TextBlock Text="v"
                                                   Foreground="#FFB7B7D6"
                                                   FontFamily="Segoe UI"
                                                   FontSize="11"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </ToggleButton>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              IsHitTestVisible="False"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Content="{TemplateBinding SelectionBoxItem}" />
                            <Popup Name="PART_Popup"
                                   Placement="Top"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade">
                                <Border Margin="0,0,0,4"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        Background="#FF111122"
                                        BorderBrush="#FF333355"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <ScrollViewer MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                                  CanContentScroll="True"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#FF5B5FC7" />
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="#FF5B5FC7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TabStripButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="FontFamily" Value="Segoe UI Semibold" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="#FFE8E8E8" />
            <Setter Property="Background" Value="#FF111122" />
            <Setter Property="BorderBrush" Value="#FF333355" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="TabStripButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabStripButtonBorder" Property="Background" Value="#FF1A1A35" />
                                <Setter TargetName="TabStripButtonBorder" Property="BorderBrush" Value="#FF5B5FC7" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="TabStripButtonBorder" Property="Background" Value="#FF24244A" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF6B6B8D" />
                                <Setter TargetName="TabStripButtonBorder" Property="Opacity" Value="0.6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BenchTabCloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="20" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Margin" Value="6,0,8,0" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Foreground" Value="#FF9A9ABD" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTip" Value="Close tab" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="CloseTabBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CloseTabBorder" Property="Background" Value="#FF24244A" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="CloseTabBorder" Property="Background" Value="#FF5B5FC7" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BenchTabItemStyle" TargetType="TabItem">
            <Setter Property="Foreground" Value="#FFB7B7D6" />
            <Setter Property="Background" Value="#FF111122" />
            <Setter Property="BorderBrush" Value="#FF333355" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                MinWidth="110"
                                Height="30"
                                Margin="0,4,6,0"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8,8,0,0"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Margin="12,0,0,0"
                                           VerticalAlignment="Center"
                                           Foreground="{TemplateBinding Foreground}"
                                           FontFamily="Segoe UI Semibold"
                                           FontSize="12"
                                           Text="{TemplateBinding Header}"
                                           TextTrimming="CharacterEllipsis" />
                                <Button Grid.Column="1"
                                        Content="&#xE8BB;"
                                        Tag="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                        Style="{StaticResource BenchTabCloseButtonStyle}"
                                        Click="OnTabCloseButtonClick" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#FF5B5FC7" />
                                <Setter TargetName="TabBorder" Property="Background" Value="#FF181832" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="White" />
                                <Setter TargetName="TabBorder" Property="Background" Value="#FF0C0C0C" />
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#FF5B5FC7" />
                                <Setter TargetName="TabBorder" Property="Panel.ZIndex" Value="5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BenchTabControlStyle" TargetType="TabControl">
            <Setter Property="Background" Value="#FF0C0C0C" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0"
                                    Background="#FF111122"
                                    BorderBrush="#FF2A2A46"
                                    BorderThickness="0,0,0,1">
                                <Grid Margin="8,0,8,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Grid Grid.Column="0" ClipToBounds="True">
                                        <TabPanel x:Name="HeaderPanel"
                                                  Margin="0,0,8,0"
                                                  Background="Transparent"
                                                  IsItemsHost="True" />
                                    </Grid>

                                    <StackPanel Grid.Column="1"
                                                Orientation="Horizontal"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center">
                                        <Button x:Name="TabStripNewTabButton"
                                                Content="+"
                                                Style="{StaticResource TabStripButtonStyle}"
                                                Click="OnNewTabClick"
                                                ToolTip="Open a new tab" />
                                        <Button x:Name="TabStripOverflowButton"
                                                Content="v"
                                                Margin="0"
                                                Style="{StaticResource TabStripButtonStyle}"
                                                Click="OnTabSwitcherClick"
                                                ToolTip="Show open tabs" />
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <Border Grid.Row="1"
                                    Background="{TemplateBinding Background}">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

        <Border Background="#FF0C0C0C"
                BorderBrush="#FF333355"
                BorderThickness="1.5"
                CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect Color="#000" BlurRadius="24" ShadowDepth="4" Opacity="0.65" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border x:Name="HeaderBar"
                    Grid.Row="0"
                    Background="#FF16162A"
                    CornerRadius="14,14,0,0"
                    MouseLeftButtonDown="OnHeaderMouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                        <TextBlock Text="Windows Clippy"
                                   Foreground="#FFCCCCCC"
                                   FontFamily="Segoe UI"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="  Copilot sessions + cards"
                                   Foreground="#FF6B6B8D"
                                   FontFamily="Segoe UI"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   Margin="2,1,0,0" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,6,0">
                        <Button x:Name="HideBenchButton"
                                Style="{StaticResource ChromeButtonStyle}"
                                Click="OnHideBenchClick"
                                Content="&#xE921;"
                                ToolTip="Hide" />
                        <Button x:Name="CloseTabButton"
                                Style="{StaticResource ChromeButtonStyle}"
                                Click="OnCloseTabClick"
                                Content="&#xE8BB;"
                                ToolTip="Close tab" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Tab content area -->
            <TabControl x:Name="Tabs"
                        Grid.Row="1"
                        Margin="0"
                        Padding="0"
                        Style="{StaticResource BenchTabControlStyle}"
                        ItemContainerStyle="{StaticResource BenchTabItemStyle}"
                        Background="#FF0C0C0C"
                        BorderThickness="0"
                        SelectionChanged="OnTabsSelectionChanged" />

            <!-- Bottom chrome: toolbar + status + input -->
            <Border Grid.Row="2"
                    Background="#FF16162A"
                    Padding="14,10"
                    CornerRadius="0,0,14,14">
                <StackPanel>
                    <!-- Toolbar row -->
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ComboBox x:Name="AgentSelector"
                                  Grid.Column="0"
                                  Width="130"
                                  Margin="0,0,6,0"
                                  Style="{StaticResource ToolbarComboBoxStyle}"
                                  SelectionChanged="OnAgentSelectionChanged"
                                  ToolTip="Choose the active Copilot agent for this tab" />

                        <ComboBox x:Name="ModelSelector"
                                  Grid.Column="1"
                                  Width="130"
                                  Margin="0,0,6,0"
                                  Style="{StaticResource ToolbarComboBoxStyle}"
                                  SelectionChanged="OnModelSelectionChanged"
                                  ToolTip="Choose the active Copilot model for this tab" />

                        <Button x:Name="ToolsBtn"
                                Grid.Column="2"
                                Content="Tools v"
                                Margin="0,0,6,0"
                                Style="{StaticResource ToolbarButtonStyle}"
                                Click="OnToolsBtnClick"
                                ToolTip="Open tool settings" />

                        <Button x:Name="ExtBtn"
                                Grid.Column="3"
                                Content="Ext v"
                                Margin="0,0,6,0"
                                Style="{StaticResource ToolbarButtonStyle}"
                                Click="OnExtBtnClick"
                                ToolTip="Open extension settings" />

                        <ToggleButton x:Name="ModeAgentToggle"
                                      Grid.Column="5"
                                      Content="A"
                                      Style="{StaticResource ModeToggleStyle}"
                                      Checked="OnModeToggleChecked"
                                      Tag="Agent"
                                      ToolTip="Agent mode" />
                        <ToggleButton x:Name="ModePlanToggle"
                                      Grid.Column="6"
                                      Content="P"
                                      Style="{StaticResource ModeToggleStyle}"
                                      Checked="OnModeToggleChecked"
                                      Tag="Plan"
                                      ToolTip="Plan mode" />
                        <ToggleButton x:Name="ModeSwarmToggle"
                                      Grid.Column="7"
                                      Content="S"
                                      Style="{StaticResource ModeToggleStyle}"
                                      Checked="OnModeToggleChecked"
                                      Tag="Swarm"
                                      ToolTip="Swarm mode" />
                    </Grid>

                    <!-- Session metadata: collapsed by default; live fleet renders in the MCP Apps WebView2 slot below. -->
                    <Grid Margin="0,0,0,4">
                        <Border x:Name="AppsHostSlot"
                                Height="140"
                                Margin="2,0,0,0"
                                Background="#110E0E28"
                                BorderBrush="#FF2A2A44"
                                BorderThickness="1"
                                CornerRadius="6" />
                        <TextBlock x:Name="SessionMeta"
                                   Visibility="Collapsed"
                                   Foreground="#FF8F8FAF"
                                   FontFamily="Segoe UI"
                                   FontSize="11"
                                   Margin="2,0,0,0"
                                   TextWrapping="Wrap" />
                    </Grid>
                    <TextBlock x:Name="AttachmentMeta"
                               Foreground="#FF6B6B8D"
                               FontFamily="Segoe UI"
                               FontSize="11"
                               Margin="2,0,0,6"
                               TextWrapping="Wrap" />

                    <!-- Input row -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="PromptLabel"
                                   Grid.Column="0"
                                   Text="Commander"
                                   Foreground="#FF5B5FC7"
                                   FontFamily="Segoe UI Semibold"
                                   FontSize="12.5"
                                   FontWeight="Bold"
                                   VerticalAlignment="Center"
                                   Margin="0,0,10,0" />
                        <TextBox x:Name="InputBox"
                                 Grid.Column="1"
                                 Background="#FF111122"
                                 Foreground="#FFE8E8E8"
                                 CaretBrush="#FF5B5FC7"
                                 FontFamily="Cascadia Code, Cascadia Mono, Consolas"
                                  FontSize="13"
                                  BorderBrush="#FF333355"
                                  BorderThickness="1"
                                  Padding="10,7"
                                  VerticalContentAlignment="Center"
                                  ToolTip="Describe the task for the active Clippy session"
                                  KeyDown="OnInputKeyDown"
                                  TextChanged="OnInputBoxTextChanged">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6" />
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <!-- Slash command autocomplete popup - floats above the InputBox -->
                        <Popup x:Name="SlashPopup"
                               Grid.Column="1"
                               PlacementTarget="{Binding ElementName=InputBox}"
                               Placement="Top"
                               StaysOpen="True"
                               AllowsTransparency="True"
                               IsOpen="False">
                            <Border Background="#FF1A1A2E"
                                    BorderBrush="#FF5B5FC7"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="0">
                                <ListBox x:Name="SlashPopupList"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         MaxHeight="220"
                                         Focusable="False"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         MouseLeftButtonUp="OnSlashPopupListMouseUp">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="Padding" Value="10,6" />
                                            <Setter Property="Foreground" Value="#FFE8E8E8" />
                                            <Setter Property="Background" Value="Transparent" />
                                            <Setter Property="FontFamily" Value="Cascadia Code, Cascadia Mono, Consolas" />
                                            <Setter Property="FontSize" Value="12" />
                                            <Setter Property="Cursor" Value="Hand" />
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FF2A2A4A" />
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="#FF3A3A6A" />
                                                    <Setter Property="Foreground" Value="White" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Command}"
                                                           Foreground="#FF7B7FE7"
                                                           FontWeight="SemiBold"
                                                           MinWidth="160" />
                                                <TextBlock Text="{Binding Description}"
                                                           Foreground="#FF8F8FAF"
                                                           Margin="8,0,0,0" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>
                        </Popup>
                        <Button x:Name="SendBtn"
                                Grid.Column="2"
                                Content="&#xE768;"
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="15"
                                Width="38"
                                Height="34"
                                Background="#FF5B5FC7"
                                Foreground="White"
                                BorderThickness="0"
                                Cursor="Hand"
                                Margin="10,0,0,0"
                                Click="OnSendClick"
                                ToolTip="Dispatch the commander prompt to the active Clippy session">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6" />
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
