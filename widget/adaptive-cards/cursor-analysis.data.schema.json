{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Clippy Cursor Analysis Data",
  "description": "Data schema for the Clippy AI cursor analysis adaptive card.",
  "type": "object",
  "required": ["title", "action", "timestamp"],
  "properties": {
    "icon": {
      "type": "string",
      "description": "URL or base64 data URI for the Clippy icon."
    },
    "title": {
      "type": "string",
      "description": "Action label (e.g. 'Explain This', 'Summarize Screen')."
    },
    "subtitle": {
      "type": "string",
      "description": "Status subtitle (e.g. 'analyzing...', 'done')."
    },
    "analysis": {
      "type": "string",
      "description": "The AI-generated analysis text."
    },
    "action": {
      "type": "string",
      "enum": ["explain", "summarize", "extract", "read-aloud", "debug", "accessibility"],
      "description": "The action type that was performed."
    },
    "cursorX": {
      "type": "integer",
      "description": "Screen X coordinate where the action was triggered."
    },
    "cursorY": {
      "type": "integer",
      "description": "Screen Y coordinate where the action was triggered."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of the capture."
    },
    "imagePath": {
      "type": "string",
      "description": "Local file path to the captured screenshot."
    },
    "contextJsonPath": {
      "type": "string",
      "description": "Local file path to the generated semantic screen-context JSON."
    },
    "contextMarkdownPath": {
      "type": "string",
      "description": "Local file path to the generated semantic screen-context Markdown report."
    },
    "contextBundlePath": {
      "type": "string",
      "description": "Local file path to the generated Paperboy bundle containing the screenshot, JSON, Markdown, and manifest."
    },
    "windowCount": {
      "type": "integer",
      "description": "Number of visible top-level windows discovered by Win32 scanning."
    },
    "interactableCount": {
      "type": "integer",
      "description": "Number of interactable UI Automation elements discovered."
    },
    "accessibilityIssueCount": {
      "type": "integer",
      "description": "Number of accessibility concerns classified from UI Automation metadata."
    },
    "ocrStatus": {
      "type": "string",
      "description": "OCR provider status for the capture, such as ok, not_configured, unavailable, or error."
    }
  }
}
